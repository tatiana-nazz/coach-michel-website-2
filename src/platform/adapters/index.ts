/**
 * Provider adapters are intentionally absent in P4-S04.
 * Later implementation authority may bind governed interfaces to approved providers.
 */
export const providerSelectionStatus = 'UNRESOLVED_BY_DESIGN' as const;
